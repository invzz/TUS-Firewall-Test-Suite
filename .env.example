# Environment variables for TUS Firewall Test Suite
# Copy this file to .env and modify as needed

# Number of client connections to simulate (default: 18)
# Increase for stress testing, decrease for quick tests
NUM_CLIENTS=18

# Duration of simulation in seconds (default: 120 = 2 minutes)  
# Longer durations generate more comprehensive traffic patterns
DURATION=120

# Examples for different test scenarios:
# Use dedicated environment files for predefined configurations:
#
# Light testing (quick validation):
#   docker-compose --env-file .env.light up
#
# Normal testing (realistic game server load):
#   docker-compose --env-file .env.normal up
#
# Stress testing (high load scenario):
#   docker-compose --env-file .env.stress up
#
# Performance testing (very high load):
#   docker-compose --env-file .env.performance up
#
# Connections per player - concurrent connections each client creates (default: 3)
# Higher values create more concurrent connections for increased load
# Total concurrent connections = NUM_CLIENTS × CONNECTIONS_PER_PLAYER
CONNECTIONS_PER_PLAYER=3

# Or create your own .env file with custom values:
# NUM_CLIENTS=<your_value>
# DURATION=<your_value>
# CONNECTIONS_PER_PLAYER=<your_value>