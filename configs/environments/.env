# TUS Firewall Test Suite - Default Configuration
# Main environment file with balanced settings for typical usage

# Basic Configuration
NUM_CLIENTS=25
DURATION=180
CONNECTIONS_PER_PLAYER=3
SERVER_IP=nftables-test-container

# Authentic UT Server Network Specifications
# Based on real server data - creates authentic UT network traffic
UT_UDP_OVERHEAD=28
UT_TICKRATE=85
UT_DEFAULT_NETSPEED=40000
UT_MAX_NETSPEED=100000

# Calculated Authentic Packet Sizes (netspeed/tickrate - udp_overhead)
# Default: 40k/85 - 28 = 442 bytes payload (470 total)
# Maximum: 100k/85 - 28 = 1148 bytes payload (1176 total)

# Alternative Environment Files Available:
# .env.light      - Quick testing (5 clients, 30s)
# .env.normal     - Balanced testing (25 clients, 180s)
# .env.stress     - High load testing (100 clients, 300s) 
# .env.performance - Maximum load testing (500 clients, 600s)
# .env.ut-specs   - Authentic UT server specifications focus

# Usage Examples:
# docker-compose --env-file .env.light up
# docker-compose --env-file .env.stress up
# docker-compose --env-file .env.ut-specs -f docker/docker-compose-game.yml up